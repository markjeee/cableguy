auth:
  require_2fa: {require_2fa}
  twitter_auth: {twitter_auth}

database:
  adapter: mysql
  host: {db.host}
  port: {db.port}
  username: {db.username}
  password: {db.password}

escaped:
  not_escaped: <%= '1' %>
  escaped: [%= runtime_test_value %]
